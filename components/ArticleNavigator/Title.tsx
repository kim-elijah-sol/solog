import styled from '@emotion/styled'
import { staticColor } from '@styles/palette'

const Title = styled.p({
  fontWeight: 500,
  color: staticColor.primary_900,
  fontSize: '0.875rem',
})

export default Title
