import styled from '@emotion/styled'

const Name = styled.h1({
  fontSize: '2.5rem',
  marginBottom: '3rem',
  '@media (max-width: 680px)': {
    fontSize: '2rem',
    marginBottom: '2.5rem',
  },
})

export default Name
